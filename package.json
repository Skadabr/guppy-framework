{
  "name": "guppy",
  "version": "1.0.3",
  "description": "Guppy Framework",
  "repository": {
    "type": "git",
    "url": "https://github.com/nexode/guppy-framework.git"
  },
  "scripts": {
    "build": "tsc",
    "coverage": "tsc && istanbul cover _mocha -- --recursive ./dist/tests && remap-istanbul -i ./coverage/coverage.json -o ./coverage/html-report -t html",
    "test": "tsc && mocha --recursive ./dist/tests"
  },
  "main": "index.js",
  "author": "Karabutin Alex <karabutinalex@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "istanbul": "^0.4.5",
    "mocha": "^3.1.2",
    "node-fetch": "^1.6.1",
    "remap-istanbul": "^0.7.0"
  },
  "dependencies": {
    "colors": "^1.1.2",
    "easy-table": "^1.0.0",
    "glob": "^7.0.6",
    "reflect-metadata": "^0.1.8"
  }
}